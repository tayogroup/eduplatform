# compose-lessons.py output. build-all.sh composes these as its first step and
# build-lessons.py reads them; every change goes in the tracked sources they are
# composed from. Measured 2026-09-11: composing reproduces all sixteen exactly.
c-*-body.html
c-*-slides.js
