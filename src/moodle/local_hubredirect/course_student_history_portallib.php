<?php
// Course-student-history query library for the token-gated portal endpoint.
// course_student_history.php defines no functions of its own (its history
// query runs inline and is ported verbatim into the course-student-history
// portal handler), so this lib is guard-only; the pqcshl_ prefix stays
// reserved for it. The legacy page stays untouched (parallel-run).

defined('MOODLE_INTERNAL') || die();

// course_student_history.php defines no functions — nothing to extract.
