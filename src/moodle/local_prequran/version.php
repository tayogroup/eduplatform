<?php
defined('MOODLE_INTERNAL') || die();

$plugin->component = 'local_prequran';
$plugin->version = 202608280032; // grade cohorts: `grade` on group_pool + class_group (the primary_grade_levels vocabulary student_profile.current_grade already uses), so a class group can BE "Grade 1, Group A" studying every subject rather than one group per course — one live teacher takes two groups of nine across the whole timetable. Nullable and unmigrated: a pool with no grade matches exactly as before. (031 = tutoring anchors: per-subject anchor stage for the tutoring-support category (local_prequran_tutoring_anchor), so the +/-2 help window is drawn around the learner's level IN THAT SUBJECT rather than one declared school year for all six; empty table keeps the previous behaviour exactly. (030 = student availability: availability_json on student_profile as the canonical structured copy matching reads (admin student-intake now persists the slot grid it was already collecting instead of flattening it to display text), backfilled from newest transferred intake request. (029 = teacher shifts: shift column on teacher_profile (shift1 day 10:00-20:00 EAT / shift2 night 20:00-06:00 EAT), matching availability = declared hours ∩ shift window, cohort teacher ranking now balances by current weekly teaching load. (028 = course cohorts: offeringid/moodlecourseid on group_pool + class_group (a group can finally BE "Grade 2 English — Nairobi cohort"), sessions_per_week + session_minutes on course_offering so availability matching knows what it must place. (027 = objective import: unit_title column, FIXED unit-number parse (unitId eng-g01-t01-u01 yielded 10101 not 1), tenant guard now understands catalog courses (was offering-only), CLI importer from the Bunny content tree. (026 = course syllabus: authored narrative (teacher-written, admin-approved, per-year versioned) + generated schedule assembled from objectives/homework/terms/gradebook; keyed on moodlecourseid so it serves BOTH course systems; legacy course_offering.syllabus migrated in. (025 = implementation audit: tenant-isolation guards on student-targeting writes (demographics/status/report-card/behaviour/grant), objective workspaceid scoping + backfill, per-workspace payout checks, exception-disclosure gate, N+1 collapses (analytics-trends, at-risk, objective coverage), SEB imagedata blob off the list query, live_attendance.studentid + market_payout.teacherid indexes, DR: port manual-SQL-only SEB/speakrec/submitrec tables into versioned schema. (024 = content authoring)
$plugin->requires = 2022041900;
$plugin->maturity = MATURITY_ALPHA;
$plugin->release = '0.5.1-tutoring-anchors';
