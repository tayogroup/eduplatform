; Make PHP re-check file timestamps on every request in this directory so
; deployed changes take effect immediately instead of serving stale OPcache.
opcache.validate_timestamps=1
opcache.revalidate_freq=0
